export const Green = ({children}) => {

    const style = {
        color: "#136942"
    }

    return (
        <span style={style}>{children}</span>
    );
}

export const Purple = ({children}) => {

    const style = {
        color: "#9900FF"
    }

    return (
        <span style={style}>{children}</span>
    );
}